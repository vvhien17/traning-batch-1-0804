import { activityQuery } from "@components/hooks/activity";
import Image from "next/image";

export default function Home() {
  // const { data } = activityQuery.query.useGetActivities({ category: "test" });
  return <main></main>;
}
