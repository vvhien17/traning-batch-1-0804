{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "git": {
    "enabled": true,
    "deployments": {
      "production": ["release"]
    }
  },
  "ignoredBuildStep": "if git log -1 --pretty=%B | grep -q '[ci skip]'; then exit 0; else exit 1; fi"
}

